# Production environment variables template
# NOTE: This file is NOT used in production (Kubernetes injects env vars directly)
# This is just for reference - copy these variables to your Kubernetes ConfigMap/Secrets

# Application
PORT=8080

# CORS Configuration (strict for production)
# Examples: 
#   Single origin: https://your-prod-domain.com
#   Multiple origins: https://your-prod-domain.com,https://app.example.com
CORS_ALLOWED_ORIGINS=https://your-prod-domain.com
CORS_ALLOW_CREDENTIALS=true

# Backend Services URLs
# These should point to Kubernetes service names (ClusterIP) in production
# Format: http://<service-name>:<port>
SERVICE_ACCESS_URL=http://shopifake-access:8080
SERVICE_AUDIT_URL=http://shopifake-audit:8080
SERVICE_CATALOG_URL=http://shopifake-catalog:8080
SERVICE_CUSTOMERS_URL=http://shopifake-customers:8080
SERVICE_INVENTORY_URL=http://shopifake-inventory:8080
SERVICE_ORDERS_URL=http://shopifake-orders:8080
SERVICE_PRICING_URL=http://shopifake-pricing:8080
SERVICE_RECOMMENDER_URL=http://shopifake-recommender:8080
SERVICE_SALES_DASHBOARD_URL=http://shopifake-sales-dashboard:8080
SERVICE_SITES_URL=http://shopifake-sites:8080
SERVICE_CHATBOT_URL=http://shopifake-chatbot:8080
SERVICE_AUTH_B2C_URL=http://shopifake-auth-b2c:3000
SERVICE_AUTH_B2E_URL=http://shopifake-auth-b2e:3001
